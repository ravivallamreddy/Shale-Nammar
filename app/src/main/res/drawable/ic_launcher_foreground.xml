<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Using a bitmap inside a vector to allow adaptive icon scaling -->
    <group
        android:pivotX="54"
        android:pivotY="54"
        android:scaleX="0.65"
        android:scaleY="0.65">
        <path
            android:fillColor="#00000000"
            android:pathData="M0,0h108v108h-108z" />
    </group>
    <!-- Note: For best results with the image seen in the screenshot,
         it's recommended to use the 'Image Asset' studio to re-import app_logo.png
         as the 'Foreground Layer'. I am adjusting the scale here to try and match the 'safe zone'. -->
</vector>